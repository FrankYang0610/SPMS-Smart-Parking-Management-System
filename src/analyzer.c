#include "state.h"
#include "vector.h"

void print_bookings(Vector* queues[], Statistics* stats[], Tracker* trackers[], int* invalid_cnt) {
    // UNIMPLEMENTED
    (void)queues;
    (void)stats;
    (void)trackers;
    (void)invalid_cnt;
}